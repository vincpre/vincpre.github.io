{
  "name": "capacity",
  "private": true,
  "version": "0.0.1",
  "description": "Import OPS metrics",
  "license": "UNLICENSED",
  "scripts": {
    "test": "jest --forceExit",
    "lint": "eslint --quiet --config .eslintrc.json --ext js --ignore-pattern dist/ --ignore-pattern coverage/ --ignore-pattern node_modules/ --ignore-pattern doc/ ."
  },
  "devDependencies": {
    "eslint": "^7.24.0",
    "eslint-config-airbnb-base": "^14.2.1",
    "eslint-plugin-import": "^2.22.1",
    "jest": "^26.6.3",
    "jest-junit": "^12.0.0",
    "npm-run-all": "^4.1.5",
    "rimraf": "^3.0.2"
  },
  "dependencies": {
    "underscore":"1.13.1"
  }
}
